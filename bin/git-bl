{
    printf 'BRANCH\tUPSTREAM\tDESCRIPTION\n'

    for BR in $(git branch --format "%(refname:lstrip=2)"); do
        echo "$BR\t$(git branch --list --format "%(upstream:remotename)" $BR)\t$(git config branch.$BR.description)"
    done
} | column -t -s $'\t'
